Считывание данных в масиве
разделение на функции
стиль
убрать копипаст
обнуление массивов

+баллы:
проверки
      tolerance
      выделение памяти
      открыт ли файл
github
динамические массивы
gnuplot



void myfunctions_free()
{
	myfree(I);
	myfree(U);
	myfree(R);
	myfree(r);
}

void myfree()
{
	free()
}
